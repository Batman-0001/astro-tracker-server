# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://astral_admin:astral_secret_2024@localhost:27017/astral_neo?authSource=admin

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# NASA API Configuration
# Get your free API key at: https://api.nasa.gov/
NASA_API_KEY=DEMO_KEY
NASA_API_BASE_URL=https://api.nasa.gov/neo/rest/v1

# CORS Configuration
# Comma-separated list of allowed frontend origins
# For production (Vercel): e.g. https://your-app.vercel.app
CORS_ORIGIN=http://localhost:3000

# Socket.IO Configuration (deprecated, use CORS_ORIGIN instead)
SOCKET_CORS_ORIGIN=http://localhost:3000
